# Chain of Verification

- Проверяемый черновик или утверждение:

Нагрузочные проверки из practices/practice_01/tests_load.md как проверяемый черновик. CASE.md — единственный источник подтверждённых требований.

## Запрос на проверку

Практика 2. Выполни эксперимент Chain of Verification над слабым артефактом practices/practice_01/tests_load.md. Исходный файл изменять запрещено. Создай его улучшенную копию practices/practice_02/chain_of_verification/tests_load_candidate.md. Примени подход Draft -> Verify -> Correct. Сначала считай текущий tests_load.md проверяемым черновиком. Затем отдельно проверь все существенные числовые значения, пороги и критерии: откуда взялось значение, подтверждается ли оно CASE.md, является ли оно обязательным требованием, параметром конкретного тестового прогона или инженерной гипотезой, и можно ли его воспроизводимо проверить. Особое внимание удели значениям вроде лимита 20 000 символов, timeout 10 секунд, RPS, длительности прогона, concurrency, p95 и success rate. Если значение подтверждено источником, укажи evidence. Если источника нет, не придумывай его и не выдавай значение за SLO: классифицируй его как параметр эксперимента, гипотезу или TBD. После верификации создай исправленный practices/practice_02/chain_of_verification/tests_load_candidate.md, явно разделив confirmed requirements, test parameters и hypotheses. Затем полностью заполни существующий practices/practice_02/chain_of_verification/experiment.md строго по его текущему шаблону, не удаляя и не переименовывая существующие разделы: укажи проверяемый черновик, полный текст этого запроса, вопросы верификации, результаты проверки и evidence, исправленный результат, созданный tests_load_candidate.md, конкретные изменения, как они проверены и что было отклонено как неподтверждённое. Не записывай скрытую chain-of-thought, фиксируй только наблюдаемые вопросы, evidence и результаты. Разрешено создавать или изменять только practices/practice_02/chain_of_verification/tests_load_candidate.md и practices/practice_02/chain_of_verification/experiment.md. Не изменяй Practice 1, не используй web, shell и Git и не придумывай новые SLO, SLA или требования.

## Вопросы проверки и evidence

| Вопрос | Источник или проверка | Результат |
|---|---|---|
| Подтверждён ли лимит 20 000 символов для diff и статус 413 для превышения? | CASE.md `API-1`; сопоставление с черновиком. | Да, подтверждено правилом `API-1`. В candidate сохранён как [ТРЕБОВАНИЕ], для oversized 100% HTTP 413. |
| Подтверждён ли timeout 10 секунд и контролируемый ответ при ошибке внешнего LLM? | CASE.md `REL-1`; проверка формулировки. | Да, подтверждено правилом `REL-1`. В candidate — [ТРЕБОВАНИЕ]; время «≤ 11 сек» отмечено как [ГИПОТЕЗА]. |
| Подтверждены ли RPS (50/10/5) и длительности прогона (10/2 минут), а также concurrency 20? | Поиск числовых целей в CASE.md. | Не подтверждены CASE.md; классифицированы как [ПАРАМЕТР] конкретного прогона. |
| Подтверждены ли p95 пороги (200 мс, 500 мс) и success ≥ 99%? | Поиск SLO/порогов в CASE.md. | Не подтверждены; это не SLO. В candidate помечены как [ГИПОТЕЗА] для наблюдения, без pass/fail. |
| Является ли требованием отсутствие LLM-вызовов для oversized запросов? | CASE.md `API-1` не диктует внутренние вызовы. | Прямого требования нет; «0 LLM calls» — [ГИПОТЕЗА]. Проверяется счётчиком LLM‑spy. |
| Подтверждена ли безопасность логов (нет секретов, только metadata)? | CASE.md `SEC-1`, `OBS-1`. | Да, подтверждено; в candidate — [ТРЕБОВАНИЕ]. Проверка: log scan с маркерами, LLM‑spy не видит секретов. |
| Требуется ли стабильность памяти (RSS не растёт монотонно) как критерий? | CASE.md не задаёт такие пороги. | Нет, это [ГИПОТЕЗА] о поведении; фиксируем наблюдение без pass/fail. |
| Нужно ли использовать реальный внешний LLM в нагрузке? | Политика не задана в CASE.md. | Нет обязательства. В candidate: регулярные прогоны — со stub/spy; staging с реальным LLM — только по отдельному согласованию. |

## Исправленный результат

Создан воспроизводимый план нагрузочных проверок (candidate), который:

- сохраняет подтверждённые требования CASE.md (`API-1`, `REL-1`, `SEC-1`, `OBS-1`) и отделяет их от параметров и гипотез;
- убирает трактовку p95/success как SLO; фиксирует их только как наблюдаемые [ГИПОТЕЗЫ];
- оставляет RPS, длительности и concurrency как [ПАРАМЕТРЫ] конкретных прогонов;
- устанавливает pass/fail только для требований из CASE.md; для гипотез — сбор метрик и описание evidence;
- документирует использование stub/spy вместо реального LLM в регулярных прогонах.

Candidate сохранён в practices/practice_02/chain_of_verification/tests_load_candidate.md.

## Созданный файл

- practices/practice_02/chain_of_verification/tests_load_candidate.md

## Что изменилось

- Создан candidate для tests_load на базе черновика practices/practice_01/tests_load.md с явной классификацией «ТРЕБОВАНИЕ/ПАРАМЕТР/ГИПОТЕЗА».
- Уточнены критерии: pass/fail только по `API-1`, `REL-1`, `SEC-1`, `OBS-1`; p95/success/RSS — наблюдаемые гипотезы.
- Добавлены ссылки на evidence и способы проверки (LLM‑spy, log scan, application metrics, отчёт нагрузки).
- Явно зафиксировано использование stub/spy и исключение реального LLM из регулярных прогонов.

## Как проверили

- Сопоставили все числовые значения черновика с CASE.md: подтвердили только `API-1` (20 000/413) и `REL-1` (timeout 10s, контролируемый ответ); `SEC-1`/`OBS-1` — требования безопасности и наблюдаемости.
- Зафиксировали RPS, длительности и concurrency как параметры прогонов, p95/success/RSS — как гипотезы; отсутствуют в CASE.md.
- Проверили, что pass/fail критерии присутствуют только у требований; для гипотез оставлены методы сбора evidence без статуса SLO.
- Проверили отсутствие новых SLO/SLA/target‑порогов в candidate.

## Что отклонили и почему

- Любые конкретные SLO/целевые RPS/latency/availability — отсутствуют в CASE.md; не вводились.
- Жёсткие pass‑критерии для p95/success/RSS — не подтверждены; оставлены как гипотезы.
- Использование реального внешнего LLM в регулярной нагрузке — отклонено в пользу stub/spy по соображениям стоимости и безопасности (не требование CASE.md).

Примечание: practices/practice_01/tests_load.md не изменялся.
